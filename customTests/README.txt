worstFit1: 

  This test is a simple sequence of memory allocation and should be
  the same whatever the allocation algorithm you use.

worstFit2:

  This test checks if allocater usues worst fit to allocate memory.

worstFit3:

  This test checks if allocator uses worst fit and merges two adjascent free chunks.

bestFit2:

  This test checks if allocater usues best fit to allocate memory.

bestFit3:

  This test checks if allocator uses best fit and merges two adjascent free chunks.